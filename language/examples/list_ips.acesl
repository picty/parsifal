#!/home/yeye/dev/FAceSL/facesl.native

list_ips := function (path) {
  f := open (path);
  while f do
    a := parse ("answer_dump", f);
    print (a.ip, a.port, length (a.content));
   done
};

map (list_ips, args);
