open Map_lexer

let keywords = ["if"; "then"; "else"; "fi";
		"print"; "filter"]

let parse_sexp s = match s with parser
  | [< ' TString tl >] -> 
  | 

let parse_bexp s = match s with parser
  | [< >]
